/**
 * TYPES
 * 
 * 1-grammarAndTypes
 *  03-types.js
 */

 //Boolean
 var on = true;
 console.log(on);

 let off = false;
 console.log(off);

 //boolean can represent: true/false, yes/no, on/off, 0/1

//Undefined means a variable has been declared, but it has not been assigned a value

let grass;
console.log(grass);

//NULL - means a variable has been declared and assigned a value of NULL

var empty = null;
console.log(empty);

/*
Null & undefined both represent variables with no value inside. Think of it this way - null values are for gag gifts 
that are boxes intentionally given with nothing inside ( because that's the gag). Undefined values are gifts 
given when the person giving the gift has forgotten to put the gift inside.
*/

let myAge = 90;
let precise = 99999999999;
console.log(precise);

let rounded = 99999999999999999;
console.log(rounded);


